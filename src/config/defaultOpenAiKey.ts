/**
 * Built-in OpenAI API key so the extension works out of the box (like Copilot).
 * Users do not need to add any key; optional: they can set their own in Settings
 * (smartdevide.models.openai.apiKey) and it will be used instead.
 */
// Set your key here for out-of-the-box use (like Copilot). Leave empty to require users to set smartdevide.models.openai.apiKey in Settings.
export const DEFAULT_OPENAI_API_KEY = '';
